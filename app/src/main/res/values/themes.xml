<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MedSync" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MedSync" parent="Base.Theme.MedSync" />
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Background color of the splash screen -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- The icon in the center (Vector drawable) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo_large_foreground</item>
        <!-- The theme to switch to after the splash is done -->
        <item name="postSplashScreenTheme">@style/Theme.MedSync</item>
    </style>
</resources>