<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA"
    tools:context=".activities.receptionist.RoomDetails">

    <include
        android:id="@+id/navbar"
        layout="@layout/layout_navbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/footer"
        app:layout_constraintTop_toBottomOf="@id/navbar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"
                android:fontFamily="sans-serif-medium"
                android:text="Room Details"
                android:textColor="#000000"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginTop="20dp"
                android:text="General Configuration"
                android:textColor="#9E9E9E"
                android:textSize="14sp"
                android:textStyle="bold" />

            <!-- Room Number -->
            <include
                android:id="@+id/card_room_number"
                layout="@layout/view_editable_input" />

            <!-- Room Type (Will handle Dropdown in Java) -->
            <include
                android:id="@+id/card_room_type"
                layout="@layout/view_editable_input" />

            <!-- Passkey -->
            <include
                android:id="@+id/card_room_passkey"
                layout="@layout/view_editable_input" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_delete_room"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginStart="20dp"
                android:layout_marginEnd="20dp"
                android:backgroundTint="#FFEBEE"
                android:text="Delete Room"
                android:textAllCaps="false"
                android:textColor="#D32F2F"
                app:cornerRadius="28dp"
                app:icon="@drawable/ic_delete"
                app:iconGravity="textStart"
                app:iconTint="#D32F2F"
                app:strokeColor="#FFCDD2"
                app:strokeWidth="1dp"
                android:visibility="visible" /> <!-- Hidden by default, show if editing -->

        </LinearLayout>
    </ScrollView>

    <include
        android:id="@+id/footer"
        layout="@layout/layout_footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>